# Google Analytics 4 measurement ID (looks like G-XXXXXXXXXX).
# Copy this file to .env.production, fill it in, then run `npm run build`.
# Empty = analytics is completely off (no script, no cookies, no requests).
VITE_GA_MEASUREMENT_ID=
